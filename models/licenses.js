const mongoose = require('mongoose');

const licensesSchema = new mongoose.Schema({
	title: {
		type: String,
	},
	slug: {
		type: String,
	},
	description: {
		type: String,
	},
	copy: {
		type: String,
	},
});

module.exports = mongoose.model('Licenses', licensesSchema, 'Licenses');
