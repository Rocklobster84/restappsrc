<!DOCTYPE html>
<html lang="en">
<head>

    <!-- META SETTINGS -->
    <% include ../partials/meta-settings.ejs %>

    <!-- PAGE TITLE -->
    <title>Edit a Webinar</title>

    <!-- CSS FILES -->
    <% include ../partials/css-includes %>

</head>

<body id="page-top">

<!-- TOP NAVIGATION -->
<% include ../partials/navbar %>

<div class="container">
    <div class="row">

        <div class="col-lg-8">

            <h1>Select a Post to Edit or Delete</h1>

            <% if(error && error.length > 0){ %>
                <div class="alert dark-red" role="alert">
                    <strong>
                        <%= error %>
                    </strong>
                </div>
            <% } %>
            <% if(success && success.length > 0){ %>
                <div class="alert green" role="alert">
                    <strong>
                        <%= success %>
                    </strong>
                </div>
            <% } %>

                <% webinars.forEach(function(allWebinars){ %>
                    <div class="row solution spacer">
                        <div class="col-lg-12 col-md-12 col-sm-12">
                            <div class="media-body">
                                <h3><%= allWebinars.title %></h3>
                                <p>
                                    <%= allWebinars.abstract %>
                                </p>
                                <p>
                                    <a href="<%= allWebinars._id %>" class="btn btn-success" role="button">View Page</a>
                                </p>
                                <p>
                                    <a href="./edit/<%= allWebinars._id %>" class="btn btn-primary" role="button">Edit</a>
                                </p>
                                <form id="delete" action="../webinars/<%= allWebinars._id %>?_method=DELETE" method="POST">
                                    <button type="submit" class="btn btn-danger">Delete</button>
                                </form>

                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>

        </div>
</div>


<!-- FOOTER -->
<% include ../partials/footer %>

<!-- JAVASCRIPT -->
<% include ../partials/js-includes %>

</body>
</html>
